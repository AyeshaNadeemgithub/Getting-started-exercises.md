"use strict";
// this exercise is similar with the last one
let vehicles_name = ["mercedes", "motorbike", "bicycle", "tesla"];
for (let i = 0; i < vehicles_name.length; i++) {
    let statement = `I would like to own a ${vehicles_name[i]}.`;
    console.log(statement);
}
